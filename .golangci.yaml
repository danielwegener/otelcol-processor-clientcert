version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused
    - gosimple
    - bodyclose
    - noctx
  settings:
    misspell:
      locale: US
  exclusions:
    rules:
      - path: _test\.go
        linters: [errcheck]
